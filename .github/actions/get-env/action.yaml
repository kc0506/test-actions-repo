name: 'Get .env'
description: 'Recover .env from secrets'
inputs:
  env-base64: 
    description: 'Base64 encoded .env file'
    required: true
# outputs:
  # random-number:
  #   description: "Random number"
  #   value: ${{ steps.random-number-generator.outputs.random-number }}
runs:
  using: "composite"
  steps:
    - run: echo ${{ inputs.env-base64 }} > .env
      shell: bash
      
